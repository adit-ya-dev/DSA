Text Data (str)

Strings are used to store text or characters in Python.
Anything written inside quotes (single or double) is treated as a string.

Example Program

name = "Akash"
message = 'Learning Python'

print(type(name))
print(message)
